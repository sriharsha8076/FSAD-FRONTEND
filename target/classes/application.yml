spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fsad_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Harsha@0821
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

jwt:
  secret: aVeryLongSecretKeyThatNeedsToBeAtLeast256BitsLongForHS256Algorithm
  expiration: 86400000 # 24 hours in milliseconds

server:
  port: 8080
