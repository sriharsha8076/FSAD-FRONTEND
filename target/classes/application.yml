spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fsad_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Harsha@0821
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: 2400031017cse1@gmail.com
    password: wvlxjclmlogvzqhz
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 10000
          timeout: 10000
          writetimeout: 10000

jwt:
  secret: ${JWT_SECRET:aVeryLongSecretKeyThatNeedsToBeAtLeast256BitsLongForHS256Algorithm}
  expiration: 86400000

otp:
  expiry-minutes: 5

mfa:
  encryption:
    key: FSAD2024MFASecure32CharKeyABC1234
  reauth:
    window-minutes: 30

server:
  port: ${PORT:8080}
